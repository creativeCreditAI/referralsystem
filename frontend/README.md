# Referral Frontend

## Setup

```bash
npm install
npm run dev
```

## Environment

Create a `.env` file with:

```bash
VITE_API_BASE_URL=http://localhost:8000/api
```

## Routes

- `/` — create a referral link
- `/signup?ref=TOKEN` — signup using a referral token
